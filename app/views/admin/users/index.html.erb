<h1>All Users</h1>

<% @users.each do |user| %>
  <section id='user-<%= user.id %>'>
    <h2><%= link_to user.name, "/admin/users/#{user.id}" %></h2>
    <p><%= user.created_at %></p>
    <p><%= user.role %></p>
  </section>
<% end %>
